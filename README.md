# checkpoint-project

Task 1
<!-- phase -1

Build a command-line datasheet reader. Given a CSV of equipment tags (e.g. tag, service, design pressure, design temperature), your script should: load the file, let the user search by tag, print all attributes for that tag, and report how many tags exceed a pressure threshold the user types in. Handle a missing file and a bad tag gracefully.
Why this one: it is a toy version of what Pathnovo does all day — read structured engineering data, look things up by tag, flag values that break a rule. -->
